import { WebViewSurface } from '@/features/webview/WebViewSurface';

// S23 Library browse — WebView chrome around /m/library.
export default function LibraryRoute() {
  return <WebViewSurface surface="library" />;
}
