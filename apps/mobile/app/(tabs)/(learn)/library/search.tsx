import { WebViewSurface } from '@/features/webview/WebViewSurface';

// S24 Library search — WebView chrome around /m/library/search.
export default function LibrarySearchRoute() {
  return <WebViewSurface surface="library-search" />;
}
