import { WebViewSurface } from '@/features/webview/WebViewSurface';

// S30 Relationship Health Check — WebView chrome around /m/relationship-health.
export default function RelationshipRoute() {
  return <WebViewSurface surface="relationship-health" />;
}
