import { WebViewSurface } from '@/features/webview/WebViewSurface';

// S31 Med Tracker — WebView chrome around /m/med-tracker.
export default function MedTrackerRoute() {
  return <WebViewSurface surface="med-tracker" />;
}
